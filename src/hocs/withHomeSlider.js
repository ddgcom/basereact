import React from 'react'

import DEFAULT_IMG from '../../../assets/banner-home.png'

export const ListOfHomePageSliderComponent = (src = DEFAULT_IMG) => { 
  return {
      'id': 2,
      'url': '#',
      'img': src
  }
}
